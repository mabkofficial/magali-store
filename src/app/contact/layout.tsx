export const metadata = {
  title: "Contact Us",
  description: "Get in touch with Magali for product questions and order inquiries.",
};

export default function ContactLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
